(function() {

	window.onload = function() {
		function sel(query) {
			var selector = query.substr(1, 1);

	/*
			//isolate first char 
			if ( selector === '#' ) {
				//id
			} 
			else if ( selector === '.' ) {

			}
	*/
		}
		
	}



//support for stacking i.e. div p .class #id * {
	//put in to array separating at space, for loop to loop through all and narrow down selection

})();